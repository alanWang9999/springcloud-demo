server:
  port: 8882
spring:
  application:
    name: project-stock
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true #拉取注册信息
    service-url:
      defaultZone: http://localhost:9000/eureka/